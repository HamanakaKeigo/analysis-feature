from sklearn.neighbors import KernelDensity
from sklearn.model_selection import GridSearchCV
import numpy as np
import matplotlib.pyplot as plt
import pickle
import matlab
import sympy as sym
import math
import scipy



x = np.array([1,2,3])
y = np.array([4,5,6])
z = [1,2]
a = {"a":[1,2],"b":[10,20],"c":[100,200]}
print(np.array(list(a.values())))

#print(np.array(list(a.values())))
